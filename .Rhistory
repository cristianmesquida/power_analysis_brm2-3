alternative = "two.sided")
res$d
#| code-overflow: wrap
res <- pwr::pwr.t.test(n = 100,
sig.level = 0.05,
power = 0.9,
type = "two.sample",
alternative = "two.sided")
round(res$d, 2)
string <- "2b"   # one between-subject factor with two levels
n <- 527         # sample size per condition
mu <- c(24, 26)  # expected means per group
sd <- 10         # expected standard deviation (sd)
alpha <- 0.05    # alpha level
labelnames <- c("intervention", "A", "B")
# A Cohen's d of 0.2 is calculated as follows = (26-24)/10
design_result <- Superpower::ANOVA_design(design = string,
n = n,
mu = mu,
sd = sd,
labelnames = labelnames,
plot = TRUE)
string <- "3w"           # one-way within-subject factor with three levels
n <- 100                 # sample size per condition
mu <- c(26, 29, 25)      # expected means per group
sd <- 10                 # expected standard deviation (sd)
r <- 0.7                 # correlation
alpha <- 0.05            # alpha level
labelnames <- c("intervention", "A", "B", "C")
design_result <- ANOVA_design(design = string,
n = n,
mu = mu,
sd = sd,
r = r,
labelnames = labelnames,
plot = TRUE)
library(pwr)        # conduct power analysis for t-tests and correlations
library(Superpower) # conduct power analysis for factorial designs
library(emmeans)    # perform pairwise comparisons
library(TOSTER)     # conduct power analysis for equivalence tests
string <- "3w"           # one-way within-subject factor with three levels
n <- 100                 # sample size per condition
mu <- c(26, 29, 25)      # expected means per group
sd <- 10                 # expected standard deviation (sd)
r <- 0.7                 # correlation
alpha <- 0.05            # alpha level
labelnames <- c("intervention", "A", "B", "C")
design_result <- ANOVA_design(design = string,
n = n,
mu = mu,
sd = sd,
r = r,
labelnames = labelnames,
plot = TRUE)
string <- "3w"           # one-way within-subject factor with three levels
n <- 100                 # sample size per condition
mu <- c(26, 29, 25)      # expected means per group
sd <- 10                 # expected standard deviation (sd)
r <- 0.7                 # correlation
alpha <- 0.05            # alpha level
labelnames <- c("intervention", "A", "B", "C")
design_result <- ANOVA_design(design = string,
n = n,
mu = mu,
sd = sd,
r = r,
labelnames = labelnames,
plot = TRUE)
result <- ANOVA_power(design_result,
alpha_level = alpha,
nsims = 1000,
verbose = FALSE)
result$main_results
result$main_results
result$emm_results
result$pc_results
knitr::kable((result$pc_results[1:2]))
result$pc_results
knitr::kable((result$pc_results[1:2]))
result <- ANOVA_power(design_result,
alpha_level = alpha,
nsims = 1000,
verbose = FALSE)
knitr::kable((result$pc_results[1:2])) # select first two columns
kable(result$main_results)
result <- ANOVA_exact(design_result,
alpha_level = alpha,
verbose = FALSE)
kable(result$main_results)
library(pwr)        # conduct power analysis for t-tests and correlations
library(Superpower) # conduct power analysis for factorial designs
library(emmeans)    # perform pairwise comparisons
library(TOSTER)     # conduct power analysis for equivalence tests
library(knitr)      # tidy tables
result <- ANOVA_exact(design_result,
alpha_level = alpha,
verbose = FALSE)
kable(result$pc_results)
result <- ANOVA_exact(design_result,
alpha_level = alpha,
verbose = FALSE)
kable(result$main_results)
lable(result$main_results)
result <- ANOVA_exact(design_result,
alpha_level = alpha,
verbose = FALSE)
kable(result$main_results)
result <- ANOVA_power(design_result,
alpha_level = alpha,
nsims = 1000,
verbose = FALSE)
# By default, Superpower performs all possible pairwise comparisons
kable(result$pc_results) # select first two columns
string <- "6w"           # one-way within-subject factor with 6 levels
n <- 100                 # sample size per condition
mu <- c(26, 29, 25, 26, 25, 21) # expected means per group
sd <- 10                 # expected standard deviation (sd)
r <- 0.7                 # correlation
alpha <- 0.05            # alpha level
labelnames <- c("intervention", "A", "B", "C", "D", "F", "G")
design_result <- ANOVA_design(design = string,
n = n,
mu = mu,
sd = sd,
r = r,
labelnames = labelnames,
plot = FALSE) # set to TRUE to visualize pattern of means
result <- ANOVA_power(design_result,
alpha_level = alpha,
nsims = 1000,
verbose = FALSE)
kable(result$pc_results) # select first two columns
string <- "5w"           # one-way within-subject factor with 6 levels
n <- 100                 # sample size per condition
mu <- c(26, 29, 25, 26, 25) # expected means per group
sd <- 10                 # expected standard deviation (sd)
r <- 0.7                 # correlation
alpha <- 0.05            # alpha level
labelnames <- c("intervention", "A", "B", "C", "D", "F")
design_result <- ANOVA_design(design = string,
n = n,
mu = mu,
sd = sd,
r = r,
labelnames = labelnames,
plot = FALSE) # set to TRUE to visualize pattern of means
result <- ANOVA_power(design_result,
alpha_level = alpha,
nsims = 1000,
verbose = FALSE)
kable(result$pc_results) # select first two columns
string <- "4w"           # one-way within-subject factor with 6 levels
n <- 100                 # sample size per condition
mu <- c(26, 29, 25, 26) # expected means per group
sd <- 10                 # expected standard deviation (sd)
r <- 0.7                 # correlation
alpha <- 0.05            # alpha level
labelnames <- c("intervention", "A", "B", "C", "D")
design_result <- ANOVA_design(design = string,
n = n,
mu = mu,
sd = sd,
r = r,
labelnames = labelnames,
plot = FALSE) # set to TRUE to visualize pattern of means
result <- ANOVA_power(design_result,
alpha_level = alpha,
nsims = 1000,
verbose = FALSE)
kable(result$pc_results) # select first two columns
result <- ANOVA_exact(design_result,
alpha_level = alpha,
verbose = FALSE)
result$emmeans
emmeans(
result$emmeans$emmeans,
specs = ~ intervention
)
result$emmeans$emmeans
emmeans_power(result)
emmeans_power(result$pc_results)
emmeans_power(result$emm_results)
result$emm_results
result$emm_results
result <- ANOVA_exact(design_result,
alpha_level = alpha,
verbose = FALSE)
result
result$emm_results
result
design_result
library(faux)
TOSTER::powerTOSTtwo()
TOSTER::power_t_TOST()
TOSTER::power_t_TOST()
?TOSTER::power_t_TOST()
power_t_TOST(delta = 5, # the mean difference
sd = 10,
eqb = c(-0.2, 0.2),
alpha = 0.05,
power = 0.9,
type = "two.sample")
power_t_TOST(delta = 3, # the mean difference
sd = 10,
eqb = c(-0.5, 0.5),
alpha = 0.05,
power = 0.9,
type = "two.sample")
power_t_TOST(delta = 3, # the mean difference
sd = 10,
eqb = 0.5,
alpha = 0.05,
power = 0.9,
type = "two.sample")
power_t_TOST(delta = 3, # the mean difference
sd = 10,   # the standard deviation
eqb = 5,   # equivalence bounds
alpha = 0.05,
power = 0.9,
type = "two.sample")
res <- power_t_TOST(delta = 3, # the mean difference
sd = 10,   # the standard deviation
eqb = 5,   # equivalence bounds
alpha = 0.05,
power = 0.9,
type = "two.sample")
ceiling(res$n)
res$bounds
res <- power_t_TOST(delta = 3, # the mean difference
sd = 10,   # the standard deviation
eqb = 5,   # equivalence bounds
alpha = 0.05,
power = 0.9,
type = "one.sample")
ceiling(res$n)
res <- power_t_TOST(delta = 3, # the mean difference
sd = 10,   # the standard deviation
eqb = 5,   # equivalence bounds
alpha = 0.05,
power = 0.9,
type = "two.sample")
ceiling(res$n)
res <- power_t_TOST(delta = 2, # the mean difference
sd = 10,   # the standard deviation
eqb = 5,   # equivalence bounds
alpha = 0.05,
power = 0.9,
type = "two.sample")
ceiling(res$n)
res <- power_t_TOST(delta = 4, # the mean difference
sd = 10,   # the standard deviation
eqb = 5,   # equivalence bounds
alpha = 0.05,
power = 0.9,
type = "two.sample")
ceiling(res$n)
res <- power_t_TOST(delta = 4, # the mean difference
sd = 10,   # the standard deviation
eqb = 5,   # equivalence bounds
alpha = 0.05,
power = 0.9,
type = "two.sample")
ceiling(res$n)
res <- power_t_TOST(delta = 5, # the mean difference
sd = 10,   # the standard deviation
eqb = 5,   # equivalence bounds
alpha = 0.05,
power = 0.9,
type = "two.sample")
res <- power_t_TOST(delta = 3, # the mean difference
sd = 10,   # the standard deviation
eqb = 5,   # equivalence bounds
alpha = 0.05,
power = 0.9,
type = "two.sample")
ceiling(res$n)
{r}
res <- power_t_TOST(delta = 4, # the mean difference
sd = 10,   # the standard deviation
eqb = 5,   # equivalence bounds
alpha = 0.05,
power = 0.9,
type = "two.sample")
ceiling(res$n)
res <- power_t_TOST(delta = 4, # the mean difference
sd = 10,   # the standard deviation
eqb = 5,   # equivalence bounds
alpha = 0.05,
power = 0.9,
type = "two.sample")
ceiling(res$n)
res <- power_t_TOST(delta = 1, # the mean difference
sd = 10,   # the standard deviation
eqb = 3,   # equivalence bounds
alpha = 0.05,
power = 0.9,
type = "one.sample")
ceiling(res$n)
PowerTOST
library(PowerTOST)
library(PowerTOST)
install.packages("PowerTOST")
res <- power_t_TOST(delta = 1, # the mean difference
sd = 10,   # the standard deviation
low_eqbound = -3,   # equivalence bounds
high_eqbound = "Inf",
alpha = 0.05,
power = 0.9,
type = "one.sample")
res <- power_t_TOST(delta = 1, # the mean difference
sd = 10,   # the standard deviation
low_eqbound = -3,   # equivalence bounds
# high_eqbound = "Inf",
alpha = 0.05,
power = 0.9,
type = "one.sample")
design <- ANOVA_design(
design = "5w",
n = 100,
mu = c(26, 29, 25, 26, 25),
sd = 10,
r = 0.7,
labelnames = c("A", "B", "C", "D", "E"),
plot = FALSE
)
design <- ANOVA_design(
design = "5w",
n = 100,
mu = c(26, 29, 25, 26, 25),
sd = 10,
r = 0.7,
labelnames = c("A", "B", "C", "D", "E"),
plot = FALSE
)
library(faux)
library(emmeans)
# for reproducibility
set.seed(123)
# design parameters
nsim <- 1000
n <- 100
# Mean and SD per group
mu <- c(26, 29, 25, 26, 25)
sd <- 10
p_values <- numeric(nsim)
for (i in 1:nsim) {
# 1. simulate data
dat <- sim_anova(
between = list(condition = c("A", "B", "C", "D", "E")),
n = n,
mu = mu,
sd = sd,
r = r,
dv = "value",
id = "id"
)
# 2. fit model
model <- lm(value ~ condition, data = dat)
# 3. estimated marginal means
emm <- emmeans(model, ~ condition)
# 4. A vs B contrast
test <- contrast(emm, method = list("A - B" = c(1, -1, 0, 0, 0)))
# extract p-value
p_values[i] <- summary(test)$p.value
}
for (i in 1:nsim) {
# 1. simulate data
dat <- sim_design(
between = list(condition = c("A", "B", "C", "D", "E")),
n = n,
mu = mu,
sd = sd,
r = r,
dv = "value",
id = "id"
)
# 2. fit model
model <- lm(value ~ condition, data = dat)
# 3. estimated marginal means
emm <- emmeans(model, ~ condition)
# 4. A vs B contrast
test <- contrast(emm, method = list("A - B" = c(1, -1, 0, 0, 0)))
# extract p-value
p_values[i] <- summary(test)$p.value
}
sim_design(
between = list(condition = c("A", "B", "C", "D")),
n = n,
mu = mu,
sd = sd,
dv = "value",
id = "id"
)
mu <- c(33, 29, 25, 26) # group mean
sim_design(
between = list(condition = c("A", "B", "C", "D")),
n = n,
mu = mu,
sd = sd,
dv = "value",
id = "id"
)
for (i in 1:nsim) {
# 1. simulate data
dat <- sim_design(
between = list(condition = c("A", "B", "C", "D")),
n = n,
mu = mu,
sd = sd,
dv = "value",
id = "id",
plot = FALSE
)
# 2. fit model
model <- lm(value ~ condition, data = dat)
# 3. estimated marginal means
emm <- emmeans(model, ~ condition)
# 4. A vs B contrast
test <- contrast(emm, method = list("A - B" = c(1, -1, 0, 0, 0)))
# extract p-value
p_values[i] <- summary(test)$p.value
}
for (i in 1:nsim) {
# 1. simulate data
dat <- sim_design(
between = list(condition = c("A", "B", "C", "D")),
n = n,
mu = mu,
sd = sd,
dv = "value",
id = "id",
plot = FALSE
)
# 2. fit model
model <- lm(value ~ condition, data = dat)
# 3. estimated marginal means
emm <- emmeans(model, ~ condition)
# 4. A vs B contrast
test <- contrast(emm, method = list("A - B" = c(1, -1, 0, 0)))
# extract p-value
p_values[i] <- summary(test)$p.value
}
# 5. power estimate
power <- mean(p_values < 0.05)
power
for (i in 1:nsim) {
# 1. simulate data
dat <- sim_design(
between = list(condition = c("A", "B", "C", "D")),
n = n,
mu = mu,
sd = sd,
dv = "value",
id = "id",
plot = FALSE
)
# 2. fit model
model <- lm(value ~ condition, data = dat)
# 3. estimated marginal means
emm <- emmeans(model, ~ condition)
# 4. A vs B contrast
test <- contrast(emm, method = list("C - D" = c(0, 0, 1, -1)))
# extract p-value
p_values[i] <- summary(test)$p.value
}
# 5. power estimate
power <- mean(p_values < 0.05)
power
emm
for (i in 1:nsim) {
# 1. simulate data
dat <- sim_design(
between = list(condition = c("A", "B", "C", "D")),
n = n,
mu = mu,
sd = sd,
dv = "value",
id = "id",
plot = FALSE
)
# 2. fit model
model <- lm(value ~ condition, data = dat)
# 3. estimated marginal means
emm <- emmeans(model, ~ condition)
# 4. A vs B contrast
test <- contrast(emm, method = list("C - D" = c(1, 0.5, -0.5, -1)))
# extract p-value
p_values[i] <- summary(test)$p.value
}
for (i in 1:nsim) {
# 1. simulate data
dat <- sim_design(
between = list(condition = c("A", "B", "C", "D")),
n = n,
mu = mu,
sd = sd,
dv = "value",
id = "id",
plot = FALSE
)
# 2. fit model
model <- lm(value ~ condition, data = dat)
# 3. estimated marginal means
emm <- emmeans(model, ~ condition)
# 4. A vs B contrast
test <- contrast(emm, method = list("linear treand" = c(2, 1, -1, 2)))
# extract p-value
p_values[i] <- summary(test)$p.value
}
# 5. power estimate
power <- mean(p_values < 0.05)
power
